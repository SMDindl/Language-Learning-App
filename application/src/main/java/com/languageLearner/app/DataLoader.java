package com.languageLearner.app;

public class DataLoader {

    // FLOW
    // Search for "LANG", same make sure it matches VALID_LANGS
    // Find the UUID, make sure it matches up with the lang on the LANGUAGE_UUIDS map

    // Contiune within looking for GAME identifers
    // Grab the UUID for game, make sure its within the VALID_GAMES (arraylist of UUID)
    

    // load gameData

    // load questions
    
}
