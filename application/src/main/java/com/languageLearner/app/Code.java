package com.languageLearner.app;

public class Code {
    
}

// public void pickLanguage(UUID uuid) {
//     ArrayList<String> languages = gameData.getLanguages().getList(uuid);
//     if (!languages.isEmpty()) {
//         if (languages.size() == 1) {
//             setCurrentLanguage(languages.get(0));
//         } else {
//             // Handle multiple languages if needed
//             System.out.println("Multiple languages found: " + languages);
//         }
//     }
// }
